@model IEnumerable<DemoMVC.Models.Employee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ĐiaChi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NămSinh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hoten)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QueQuan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ĐiaChi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NămSinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Hoten)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QueQuan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CCCD">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CCCD">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CCCD">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
